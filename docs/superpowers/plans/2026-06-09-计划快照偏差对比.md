# 计划快照偏差对比 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户手动封存某时点的「计划累计储蓄曲线」快照,并在月度表内嵌「当时预计 / 差额」两列,逐月对比计划与实际。

**Architecture:** 在每个方案的 `PlanData` 下新增 `snapshots: PlanSnapshot[]`(与现有多方案 Workspace 兼容,快照随方案独立存储)。`useStore` 新增封存/删除/重命名快照方法;`useCalculation` 新增纯函数 `buildComparison` 把选中快照叠加到月度结果;`MonthlyTable` 顶部加快照选择器与「封存当前计划」按钮,选中后右侧追加两列。

**Tech Stack:** Vue 3 (Composition API + TypeScript)、Vitest、@vue/test-utils、UnoCSS。

**前置事实(实现者必读):**
- 当前 localStorage 顶层是 `Workspace`(`{ version:1, scenarios: Scenario[], activeId }`),每个 `Scenario.plan` 是一份 `PlanData`。
- `useStore().data` 是 computed,指向当前激活方案的 `plan`。组件里 `data.value.snapshots` 即当前方案的快照。
- `isValidPlanData` 要求 `version === 2`,被 `isValidScenario` 复用做加载校验。已有存量数据的 plan **没有** `snapshots` 字段,加载时必须容错(缺失视为 `[]`),否则会被判为坏数据清空。
- 测试命令:全部 `npm run test -- --run`;单文件 `npx vitest run <路径>`。
- Git 提交信息用中文。

---

### Task 1: 新增 PlanSnapshot 类型并挂到 PlanData

**Files:**
- Modify: `src/types.ts`

- [ ] **Step 1: 在 types.ts 中新增类型并扩展 PlanData**

在 `MonthlyAnchor` 之后、`SystemParams` 之前(或文件内任意合理位置)新增 `PlanSnapshot`,并给 `PlanData` 增加 `snapshots` 字段:

```ts
export interface PlanSnapshot {
  id: string
  name: string                          // 默认「YYYY-MM 计划」，可重命名
  createdMonth: number                  // 封存时的系统当前月（YYYYMM）
  projection: Record<number, number>    // 稀疏存储：key=YYYYMM，value=该月计划累计储蓄
}
```

把 `PlanData` 改为:

```ts
export interface PlanData {
  version: number
  systemParams: SystemParams
  columns: FlowColumn[]
  anchors: MonthlyAnchor[]
  snapshots: PlanSnapshot[]
}
```

- [ ] **Step 2: 类型检查通过(构建)**

Run: `npx vue-tsc --noEmit`
Expected: 报错若干处(`createDefault` 返回的对象缺 `snapshots`、测试里的 `makePlan` 等),这些将在后续任务修复。仅确认 `types.ts` 本身语法正确、无该文件内报错即可。

> 注:此任务故意先只改类型,后续任务补齐实现与测试数据。若希望保持每步绿色,可与 Task 2 连续完成后再跑测试。

- [ ] **Step 3: Commit**

```bash
git add src/types.ts
git commit -m "feat: 新增 PlanSnapshot 类型并扩展 PlanData"
```

---

### Task 2: useStore 支持快照存储与加载容错

**Files:**
- Modify: `src/composables/useStore.ts`
- Test: `tests/composables/useStore.spec.ts`

- [ ] **Step 1: 写失败测试 — 默认数据含空 snapshots、加载容错、封存/删除/重命名**

在 `tests/composables/useStore.spec.ts` 的 `describe('useStore', ...)` 内追加以下测试(文件已存在 `loadUseStore` 辅助与 `beforeEach` 清理):

```ts
  it('默认方案的 plan 含空 snapshots 数组', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    expect(store.data.value.snapshots).toEqual([])
  })

  it('加载缺少 snapshots 字段的存量 Workspace 时补为空数组且不清空数据', async () => {
    localStorage.setItem(
      'family-finance-plan',
      JSON.stringify({
        version: 1,
        activeId: 'sc1',
        scenarios: [
          {
            id: 'sc1',
            name: '默认方案',
            plan: {
              version: 2,
              systemParams: { startMonth: 202601, annualRate: 0.025 },
              columns: [],
              anchors: [{ month: 202601, actualSavings: 100000 }],
              // 注意：故意不含 snapshots
            },
          },
        ],
      }),
    )
    const useStore = await loadUseStore()
    const store = useStore()
    // 数据未被清空（锚点仍在），且 snapshots 补成空数组
    expect(store.data.value.anchors).toEqual([{ month: 202601, actualSavings: 100000 }])
    expect(store.data.value.snapshots).toEqual([])
  })

  it('addSnapshot 收录各月累计储蓄并按 createdMonth 命名', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    store.data.value.systemParams.startMonth = 202601
    store.data.value.systemParams.annualRate = 0
    store.addColumn('工资')
    const colId = store.data.value.columns[0].id
    store.updateColumnEntry(colId, 202601, 5000)

    const snap = store.addSnapshot()

    expect(store.data.value.snapshots).toHaveLength(1)
    expect(snap.name).toBe('2026-01 计划')
    expect(snap.createdMonth).toBe(202601)
    // 首月累计 = 5000，次月累计 = 10000（年化 0，纯累加）
    expect(snap.projection[202601]).toBe(5000)
    expect(snap.projection[202602]).toBe(10000)
  })

  it('removeSnapshot 删除指定快照', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    const snap = store.addSnapshot()
    store.removeSnapshot(snap.id)
    expect(store.data.value.snapshots).toEqual([])
  })

  it('renameSnapshot 重命名指定快照', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    const snap = store.addSnapshot()
    store.renameSnapshot(snap.id, '买房前基线')
    expect(store.data.value.snapshots[0].name).toBe('买房前基线')
  })

  it('reset 清空当前方案的 snapshots', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    store.addSnapshot()
    store.reset()
    expect(store.data.value.snapshots).toEqual([])
  })
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/composables/useStore.spec.ts`
Expected: FAIL — `store.addSnapshot is not a function`、`snapshots` 为 `undefined` 等。

- [ ] **Step 3: 实现 — createDefault 补 snapshots、校验容错、封存/删除/重命名、reset 清空**

在 `src/composables/useStore.ts` 顶部 import 增加 `calculate` 与 `formatMonth`、类型 `PlanSnapshot`:

```ts
import { computed, ref, watch, type ComputedRef, type Ref } from 'vue'
import type { PlanData, FlowColumn, Scenario, Workspace, PlanSnapshot } from '../types'
import { getCurrentMonth, formatMonth } from '../utils/month'
import { calculate } from './useCalculation'
```

`createDefault()` 增加 `snapshots: []`:

```ts
function createDefault(): PlanData {
  return {
    version: 2,
    systemParams: {
      startMonth: getCurrentMonth(),
      annualRate: 0.025,
    },
    columns: [],
    anchors: [],
    snapshots: [],
  }
}
```

新增快照校验函数(放在 `isValidAnchor` 之后):

```ts
function isValidSnapshot(value: unknown): boolean {
  if (!isObject(value)) return false
  if (typeof value.id !== 'string' || typeof value.name !== 'string') return false
  if (!isFiniteNumber(value.createdMonth)) return false
  if (!isObject(value.projection)) return false
  for (const key in value.projection) {
    if (!Number.isInteger(Number(key)) || !isFiniteNumber(value.projection[key])) return false
  }
  return true
}
```

修改 `isValidPlanData`:`snapshots` 缺失允许通过,存在则必须是合法数组(不在此处补值,补值在加载归一化里做):

```ts
function isValidPlanData(value: unknown): value is PlanData {
  if (!isObject(value) || !isObject(value.systemParams)) return false
  if (value.version !== 2) return false
  if ('snapshots' in value) {
    if (!Array.isArray(value.snapshots) || !value.snapshots.every(isValidSnapshot)) return false
  }
  return (
    isFiniteNumber(value.version) &&
    isFiniteNumber(value.systemParams.startMonth) &&
    isFiniteNumber(value.systemParams.annualRate) &&
    Array.isArray(value.columns) &&
    value.columns.every(isValidColumn) &&
    Array.isArray(value.anchors) &&
    value.anchors.every(isValidAnchor)
  )
}
```

新增归一化函数(确保每个 plan 都有 `snapshots` 数组),放在 `loadWorkspace` 之前:

```ts
// 确保每个方案的 plan 含 snapshots 数组（容错存量数据）
function normalizeWorkspace(ws: Workspace): Workspace {
  for (const scenario of ws.scenarios) {
    if (!Array.isArray(scenario.plan.snapshots)) {
      scenario.plan.snapshots = []
    }
  }
  return ws
}
```

在 `loadWorkspace` 中,所有返回 `Workspace` 的路径都套一层 `normalizeWorkspace`。改法:

```ts
function loadWorkspace(): Workspace {
  const raw = localStorage.getItem(STORAGE_KEY)
  if (!raw) {
    return createDefaultWorkspace()
  }
  try {
    const parsed: unknown = JSON.parse(raw)
    if (isValidWorkspace(parsed)) return normalizeWorkspace(parsed)
    if (isValidPlanData(parsed)) {
      const id = generateId()
      const migrated: Workspace = normalizeWorkspace({
        version: 1,
        scenarios: [{ id, name: '默认方案', plan: parsed }],
        activeId: id,
      })
      localStorage.setItem(STORAGE_KEY, JSON.stringify(migrated))
      return migrated
    }
  } catch {
    // Fall through to recovery
  }
  localStorage.removeItem(STORAGE_KEY)
  const defaultWorkspace = createDefaultWorkspace()
  localStorage.setItem(STORAGE_KEY, JSON.stringify(defaultWorkspace))
  return defaultWorkspace
}
```

> 说明:`createDefault()` 已含空 `snapshots`,`createDefaultWorkspace()` 走它,故默认路径天然合规;`normalizeWorkspace` 主要兜底存量/迁移数据。

在 `reset()` 内,`createDefault()` 已带空 `snapshots`,赋值整份 plan 即清空快照,无需额外代码。确认 `reset` 仍为:

```ts
  function reset() {
    if (saveTimeout) {
      clearTimeout(saveTimeout)
      saveTimeout = null
    }
    const defaultPlan = createDefault()
    resetSnapshot = JSON.stringify({ ...workspace.value })
    const scenario = workspace.value.scenarios.find(s => s.id === workspace.value.activeId)
    if (scenario) {
      scenario.plan = defaultPlan
    }
  }
```

新增三个快照方法(放在 `removeAnchor` 之后、`reset` 之前或之后均可):

```ts
  // 快照操作（作用于当前激活方案）
  function addSnapshot(): PlanSnapshot {
    const plan = getActivePlan()
    const results = calculate(plan)
    const projection: Record<number, number> = {}
    for (const r of results) {
      projection[r.month] = r.cumSavings
    }
    const snapshot: PlanSnapshot = {
      id: generateId(),
      name: `${formatMonth(plan.systemParams.startMonth)} 计划`,
      createdMonth: plan.systemParams.startMonth,
      projection,
    }
    plan.snapshots.push(snapshot)
    return snapshot
  }

  function removeSnapshot(id: string): void {
    const plan = getActivePlan()
    plan.snapshots = plan.snapshots.filter(s => s.id !== id)
  }

  function renameSnapshot(id: string, name: string): void {
    const plan = getActivePlan()
    const snapshot = plan.snapshots.find(s => s.id === id)
    if (snapshot) {
      snapshot.name = name
    }
  }
```

在 `return { ... }` 中导出这三个方法:

```ts
  return {
    data,
    workspace,
    save,
    addColumn,
    removeColumn,
    renameColumn,
    updateColumnEntry,
    addAnchor,
    removeAnchor,
    addSnapshot,
    removeSnapshot,
    renameSnapshot,
    reset,
    addScenario,
    duplicateScenario,
    removeScenario,
    renameScenario,
    switchScenario,
  }
```

> 注:`formatMonth(202601)` 返回 `"2026-01"`,故快照名为 `"2026-01 计划"`,与测试一致。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/composables/useStore.spec.ts`
Expected: PASS(含新增 6 个用例与原有用例)。

- [ ] **Step 5: Commit**

```bash
git add src/composables/useStore.ts tests/composables/useStore.spec.ts
git commit -m "feat: useStore 支持计划快照的封存/删除/重命名与加载容错"
```

---

### Task 3: useCalculation 新增 buildComparison

**Files:**
- Modify: `src/composables/useCalculation.ts`
- Test: `tests/composables/useCalculation.spec.ts`

- [ ] **Step 1: 写失败测试**

`tests/composables/useCalculation.spec.ts` 顶部已 `import { calculate, resolveColumnValue } from '../../src/composables/useCalculation'`。改为同时导入 `buildComparison` 与类型:

```ts
import { calculate, resolveColumnValue, buildComparison } from '../../src/composables/useCalculation'
import type { PlanData, FlowColumn, MonthResult, PlanSnapshot } from '../../src/types'
```

在文件末尾追加:

```ts
describe('buildComparison', () => {
  function makeResult(overrides: Partial<MonthResult> = {}): MonthResult {
    return {
      month: 202601,
      columnValues: [],
      totalFlow: 0,
      investReturn: 0,
      monthlyIncome: 0,
      monthlyExpense: 0,
      monthlyBalance: 0,
      cumSavings: 0,
      isAnchor: false,
      ...overrides,
    }
  }

  const results: MonthResult[] = [
    makeResult({ month: 202601, cumSavings: 5000, isAnchor: false }),
    makeResult({ month: 202602, cumSavings: 8500, isAnchor: true }),
    makeResult({ month: 202603, cumSavings: 12000, isAnchor: false }),
  ]

  const snapshot: PlanSnapshot = {
    id: 's1',
    name: '2026-01 计划',
    createdMonth: 202601,
    projection: { 202601: 5000, 202602: 9000 }, // 202603 缺失
  }

  it('snapshot 为 null 时 predicted/diff 全为 null', () => {
    const cmp = buildComparison(results, null)
    expect(cmp.map(c => c.predicted)).toEqual([null, null, null])
    expect(cmp.map(c => c.diff)).toEqual([null, null, null])
    expect(cmp.map(c => c.actual)).toEqual([5000, 8500, 12000])
  })

  it('predicted 取自快照，缺失月为 null', () => {
    const cmp = buildComparison(results, snapshot)
    expect(cmp[0].predicted).toBe(5000)
    expect(cmp[1].predicted).toBe(9000)
    expect(cmp[2].predicted).toBeNull()
  })

  it('diff 仅在该月 isAnchor 且 predicted 非空时计算', () => {
    const cmp = buildComparison(results, snapshot)
    // 202601：非 anchor → null
    expect(cmp[0].diff).toBeNull()
    // 202602：anchor 且 predicted=9000 → 8500-9000=-500
    expect(cmp[1].diff).toBe(-500)
    // 202603：非 anchor → null
    expect(cmp[2].diff).toBeNull()
  })

  it('isAnchor 但 predicted 缺失时 diff 为 null', () => {
    const anchoredNoPredict: MonthResult[] = [
      makeResult({ month: 202603, cumSavings: 12000, isAnchor: true }),
    ]
    const cmp = buildComparison(anchoredNoPredict, snapshot)
    expect(cmp[0].predicted).toBeNull()
    expect(cmp[0].diff).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/composables/useCalculation.spec.ts`
Expected: FAIL — `buildComparison is not a function`。

- [ ] **Step 3: 实现 buildComparison**

在 `src/composables/useCalculation.ts` 顶部 import 增加类型 `PlanSnapshot`,并定义结果类型与函数。文件顶部:

```ts
import type { FlowColumn, MonthResult, PlanData, PlanSnapshot } from '../types'
import { addMonths } from '../utils/month'
```

在文件末尾追加:

```ts
export interface SnapshotComparison {
  month: number
  predicted: number | null   // 该月当时预计；快照无该月则 null
  actual: number             // 实际/当前累计（= MonthResult.cumSavings）
  diff: number | null        // actual - predicted；仅当该月 isAnchor 且 predicted 非空时有值
}

/**
 * 把选中的计划快照叠加到月度结果，逐月给出预计/实际/差额。
 * @param results 当前计算出的月度结果
 * @param snapshot 选中的计划快照；null 表示未选中
 */
export function buildComparison(
  results: MonthResult[],
  snapshot: PlanSnapshot | null,
): SnapshotComparison[] {
  return results.map(r => {
    const predicted =
      snapshot && r.month in snapshot.projection ? snapshot.projection[r.month] : null
    const diff =
      predicted !== null && r.isAnchor ? r.cumSavings - predicted : null
    return {
      month: r.month,
      predicted,
      actual: r.cumSavings,
      diff,
    }
  })
}
```

> 注:`r.month in snapshot.projection` 中 `projection` 的键是字符串(JSON 对象键),`in` 对数字 `r.month` 会自动转字符串匹配,行为正确(与 `resolveColumnValue` 里 `String(month) in entries` 一致)。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/composables/useCalculation.spec.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/composables/useCalculation.ts tests/composables/useCalculation.spec.ts
git commit -m "feat: 新增 buildComparison 计算快照逐月偏差"
```

---

### Task 4: 修复其余测试中的 PlanData 字面量

**Files:**
- Modify: `tests/composables/useCalculation.spec.ts`
- 检查并按需修改: `tests/composables/useStore.spec.ts`、`tests/components/*.spec.ts`

- [ ] **Step 1: 给 makePlan 补 snapshots 默认值**

`tests/composables/useCalculation.spec.ts` 的 `makePlan` 增加 `snapshots: []`:

```ts
function makePlan(overrides: Partial<PlanData> = {}): PlanData {
  return {
    version: 2,
    systemParams: {
      startMonth: 202601,
      annualRate: 0,
    },
    columns: [],
    anchors: [],
    snapshots: [],
    ...overrides,
  }
}
```

- [ ] **Step 2: 搜索其它直接构造 PlanData 的字面量**

Run: `grep -rn "version: 2" tests/`
Expected: 列出所有内联 `PlanData`。逐一确认:凡作为 `PlanData`/`Scenario.plan` 使用且缺 `snapshots` 的字面量,补 `snapshots: []`。(`useStore.spec.ts` 中故意测试「缺 snapshots」的那条 Task 2 用例**不要**补。)

> 已知需检查处:`tests/composables/useStore.spec.ts:261` 附近的合法 Workspace 构造——若该用例断言数据被正常加载(非清空),其 plan 需补 `snapshots: []`;若它本就是测损坏数据,保持不动。实现者读取上下文判断。

- [ ] **Step 3: 运行全部测试确认通过**

Run: `npm run test -- --run`
Expected: PASS(全部用例)。

- [ ] **Step 4: Commit**

```bash
git add tests/
git commit -m "test: 补齐 PlanData 字面量的 snapshots 字段"
```

---

### Task 5: MonthlyTable 顶部快照选择器与封存按钮

**Files:**
- Modify: `src/components/MonthlyTable.vue`
- Test: `tests/components/MonthlyTable.spec.ts`

本任务只加「工具条 + 选择器 + 封存/重命名/删除」,对比列在 Task 6 加。

- [ ] **Step 1: 写失败测试**

在 `tests/components/MonthlyTable.spec.ts` 的 `describe('MonthlyTable', ...)` 内追加(沿用文件已有的 `useSharedStore` 静态导入与 `createResult`):

```ts
  it('渲染快照工具条与封存按钮', async () => {
    const useStore = await loadUseStore()
    useStore()
    const wrapper = mount(MonthlyTable, { props: { results: [createResult()] } })
    expect(wrapper.find('[aria-label="封存当前计划"]').exists()).toBe(true)
    expect(wrapper.find('[aria-label="选择对比快照"]').exists()).toBe(true)
  })

  it('点击封存按钮新增一份快照并自动选中', async () => {
    const store = useSharedStore()
    const wrapper = mount(MonthlyTable, { props: { results: [createResult({ month: 202601, cumSavings: 5000 })] } })

    await wrapper.find('[aria-label="封存当前计划"]').trigger('click')
    await nextTick()

    expect(store.data.value.snapshots).toHaveLength(1)
    // 下拉选项里出现该快照名
    const select = wrapper.find('[aria-label="选择对比快照"]')
    expect(select.text()).toContain('计划')
  })
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/components/MonthlyTable.spec.ts`
Expected: FAIL — 找不到 `[aria-label="封存当前计划"]`。

- [ ] **Step 3: 实现工具条**

在 `MonthlyTable.vue` `<script setup>` 内,`store`/`columns` 之后新增快照相关状态与方法:

```ts
import { useStore } from '../composables/useStore'
// ...已有 import...

const store = useStore()
const columns = computed(() => store.data.value.columns)

// 快照对比
const snapshots = computed(() => store.data.value.snapshots)
// 按 createdMonth 倒序（最新在前）
const sortedSnapshots = computed(() =>
  [...snapshots.value].sort((a, b) => b.createdMonth - a.createdMonth),
)
const selectedSnapshotId = ref<string | null>(null)
const selectedSnapshot = computed(
  () => snapshots.value.find(s => s.id === selectedSnapshotId.value) ?? null,
)

// 快照重命名状态
const renamingSnapshotId = ref<string | null>(null)
const snapshotRenameValue = ref<string>('')
const snapshotRenameInput = ref<HTMLInputElement | null>(null)
function setSnapshotRenameInput(el: any) { snapshotRenameInput.value = el ?? null }

function handleAddSnapshot() {
  const snap = store.addSnapshot()
  selectedSnapshotId.value = snap.id
  // 自动进入命名编辑态
  renamingSnapshotId.value = snap.id
  snapshotRenameValue.value = snap.name
}

function confirmSnapshotRename() {
  if (renamingSnapshotId.value === null) return
  const trimmed = snapshotRenameValue.value.trim()
  if (trimmed) {
    store.renameSnapshot(renamingSnapshotId.value, trimmed)
  }
  renamingSnapshotId.value = null
  snapshotRenameValue.value = ''
}

function cancelSnapshotRename() {
  renamingSnapshotId.value = null
  snapshotRenameValue.value = ''
}

function startSnapshotRename() {
  if (!selectedSnapshot.value) return
  renamingSnapshotId.value = selectedSnapshot.value.id
  snapshotRenameValue.value = selectedSnapshot.value.name
}

function handleRemoveSnapshot() {
  const snap = selectedSnapshot.value
  if (!snap) return
  if (window.confirm(`确定要删除快照"${snap.name}"吗？`)) {
    store.removeSnapshot(snap.id)
    selectedSnapshotId.value = null
  }
}
```

`useClickOutside` 已在文件中使用,追加一行复用:

```ts
useClickOutside(snapshotRenameInput, confirmSnapshotRename)
```

在 `<template>` 最外层 `<div class="h-full overflow-auto border rounded bg-white">` **之前**包一层 flex 容器并加工具条。把根元素改为:

```html
<template>
  <div class="h-full flex flex-col">
    <!-- 快照对比工具条 -->
    <div class="flex-none flex items-center gap-2 px-2 py-1 border-b bg-gray-50 text-[12px]">
      <span class="text-gray-500">计划对比</span>
      <!-- 命名编辑态 -->
      <input
        v-if="renamingSnapshotId !== null"
        :ref="setSnapshotRenameInput"
        v-focus
        type="text"
        class="border rounded px-1 text-[12px] w-32"
        :value="snapshotRenameValue"
        @input="snapshotRenameValue = ($event.target as HTMLInputElement).value"
        @keyup.enter="confirmSnapshotRename"
        @keyup.escape="cancelSnapshotRename"
        @blur="confirmSnapshotRename"
      />
      <!-- 选择器 -->
      <select
        v-else
        aria-label="选择对比快照"
        class="border rounded px-1 py-0.5 text-[12px]"
        :value="selectedSnapshotId ?? ''"
        @change="selectedSnapshotId = ($event.target as HTMLSelectElement).value || null"
      >
        <option value="">无</option>
        <option v-for="s in sortedSnapshots" :key="s.id" :value="s.id">{{ s.name }}</option>
      </select>
      <!-- 选中快照后的重命名/删除 -->
      <template v-if="selectedSnapshot && renamingSnapshotId === null">
        <button type="button" class="text-blue-600 hover:text-blue-800" aria-label="重命名快照" @click="startSnapshotRename">重命名</button>
        <button type="button" class="text-red-600 hover:text-red-800" aria-label="删除快照" @click="handleRemoveSnapshot">删除</button>
      </template>
      <button
        type="button"
        class="ml-auto px-2 py-0.5 border rounded hover:bg-white"
        aria-label="封存当前计划"
        @click="handleAddSnapshot"
      >
        封存当前计划
      </button>
    </div>

    <!-- 原表格容器 -->
    <div class="flex-1 overflow-auto border rounded bg-white">
      <table class="min-w-full border-collapse text-[11px] leading-tight">
        <!-- ...保持原有 thead/tbody 不变... -->
      </table>
    </div>
  </div>

  <FormulaPopover ... />
  <ContextMenu ... />
</template>
```

> 实现提示:只把根 `<div>` 换成 `flex flex-col` 并新增工具条 + 把原表格 `<div>` 作为 `flex-1` 子项;`<table>` 及其 `thead/tbody` 内容**不要改动**(Task 6 再加列)。`v-focus` 指令文件中已定义,可直接用。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/components/MonthlyTable.spec.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/MonthlyTable.vue tests/components/MonthlyTable.spec.ts
git commit -m "feat: 月度表新增快照选择器与封存当前计划按钮"
```

---

### Task 6: MonthlyTable 对比列(当时预计 / 差额)

**Files:**
- Modify: `src/components/MonthlyTable.vue`
- Test: `tests/components/MonthlyTable.spec.ts`

- [ ] **Step 1: 写失败测试**

追加用例(验证选中快照后出现两列表头,且差额按规则显示/留空):

```ts
  it('选中快照后显示「当时预计」「差额」两列并按规则计算', async () => {
    const store = useSharedStore()
    // 构造一份快照
    store.data.value.snapshots = [
      { id: 's1', name: '2026-01 计划', createdMonth: 202601, projection: { 202601: 5000, 202602: 9000 } },
    ]
    const results = [
      createResult({ month: 202601, cumSavings: 5000, isAnchor: false }),
      createResult({ month: 202602, cumSavings: 8500, isAnchor: true }),
    ]
    const wrapper = mount(MonthlyTable, { props: { results } })

    // 选中快照
    const select = wrapper.find('[aria-label="选择对比快照"]')
    await (select.element as HTMLSelectElement)
    select.setValue('s1')
    await nextTick()

    const headers = wrapper.findAll('th').map(c => c.text())
    expect(headers).toContain('当时预计')
    expect(headers).toContain('差额')

    // 表体含预计值 9,000 与差额 -500（202602 行，anchor）
    const bodyText = wrapper.find('tbody').text()
    expect(bodyText).toContain('9,000')
    expect(bodyText).toContain('-500')
  })

  it('未选中快照时不渲染对比列', async () => {
    const store = useSharedStore()
    store.data.value.snapshots = []
    const wrapper = mount(MonthlyTable, { props: { results: [createResult()] } })
    const headers = wrapper.findAll('th').map(c => c.text())
    expect(headers).not.toContain('当时预计')
    expect(headers).not.toContain('差额')
  })
```

> 注:`setValue` 后需 `nextTick`。若 `setValue` 对原生 select 行为异常,可改为直接设 `selectedSnapshotId`——但首选通过 DOM 交互。实现者若遇 @vue/test-utils 对 select 的兼容问题,可用 `await select.setValue('s1')` 一行替代上面两行。

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/components/MonthlyTable.spec.ts`
Expected: FAIL — 表头无「当时预计」。

- [ ] **Step 3: 实现对比列**

在 `<script setup>` 引入 `buildComparison` 并按月建索引,便于模板按 `result.month` 取对比数据:

```ts
import { calculate, buildComparison } from '../composables/useCalculation'
// 若文件未导入 calculate 可只导入 buildComparison；本组件已有 props.results，无需 calculate
```

实际只需:

```ts
import { buildComparison } from '../composables/useCalculation'

// ...在 selectedSnapshot 之后...
const comparisonByMonth = computed(() => {
  const list = buildComparison(props.results, selectedSnapshot.value)
  const map = new Map<number, { predicted: number | null; diff: number | null }>()
  for (const c of list) {
    map.set(c.month, { predicted: c.predicted, diff: c.diff })
  }
  return map
})

function getComparison(month: number) {
  return comparisonByMonth.value.get(month) ?? { predicted: null, diff: null }
}

// 差额样式：正绿、负红、0/空中性
function getDiffClass(diff: number | null): string {
  if (diff === null) return ''
  if (diff > 0) return 'text-green-600'
  if (diff < 0) return 'text-red-600'
  return 'text-gray-500'
}
```

在 `<thead>` 的最后一个固定列「余额」`<th>` 之后,新增两个仅在选中快照时显示的表头:

```html
          <th class="px-1 py-0 text-right tabular-nums font-semibold whitespace-nowrap">余额</th>
          <!-- 对比列表头 -->
          <th v-if="selectedSnapshot" class="px-1 py-0 text-right tabular-nums font-semibold whitespace-nowrap">当时预计</th>
          <th v-if="selectedSnapshot" class="px-1 py-0 text-right tabular-nums font-semibold whitespace-nowrap">差额</th>
```

在 `<tbody>` 每行最后的「累计列」`</td>` 之后,新增两个对应单元格:

```html
          <!-- 累计列 ...原有 td... -->
          </td>

          <!-- 对比列：当时预计 -->
          <td
            v-if="selectedSnapshot"
            class="px-1 py-0 text-right tabular-nums whitespace-nowrap text-gray-600"
          >
            <span v-if="getComparison(result.month).predicted !== null">
              {{ formatCurrency(getComparison(result.month).predicted as number) }}
            </span>
            <span v-else class="text-gray-300">—</span>
          </td>
          <!-- 对比列：差额 -->
          <td
            v-if="selectedSnapshot"
            class="px-1 py-0 text-right tabular-nums whitespace-nowrap"
            :class="getDiffClass(getComparison(result.month).diff)"
          >
            <span v-if="getComparison(result.month).diff !== null">
              {{ formatCurrency(getComparison(result.month).diff as number) }}
            </span>
          </td>
```

> 注:`formatCurrency(-500)` 返回 `"-500"`,负号天然带出;差额为 `null`(未发生月份)时单元格留空,符合设计。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/components/MonthlyTable.spec.ts`
Expected: PASS。

- [ ] **Step 5: 运行全部测试 + 构建**

Run: `npm run test -- --run && npm run build`
Expected: 测试全绿;`vue-tsc` 类型检查与 `vite build` 均成功。

- [ ] **Step 6: Commit**

```bash
git add src/components/MonthlyTable.vue tests/components/MonthlyTable.spec.ts
git commit -m "feat: 月度表新增当时预计与差额对比列"
```

---

## 自检对照(Spec Coverage)

- ✅ 数据结构 `PlanSnapshot` + `PlanData.snapshots` — Task 1
- ✅ 加载容错(缺失补 `[]`、非法拒绝) — Task 2(`isValidSnapshot`/`normalizeWorkspace`)
- ✅ 封存 `addSnapshot`(收录各月 `cumSavings`、按 `createdMonth` 命名) — Task 2
- ✅ `removeSnapshot`/`renameSnapshot`、`reset` 清空 — Task 2
- ✅ `buildComparison`(predicted 缺失为 null;diff 仅 isAnchor 且 predicted 非空;snapshot=null 全留空) — Task 3
- ✅ 月度表选择器 + 封存按钮(封存后自动选中并进命名态) — Task 5
- ✅ 选中快照重命名/删除入口 — Task 5
- ✅ 「当时预计 / 差额」两列(正绿负红、未发生留空、未选隐藏) — Task 6
- ✅ 计算示例口径(实际−预计、未来月留空)与测试断言一致 — Task 3/6
- ✅ 不做图表/自动封存/现金流下钻 — 计划未涉及(YAGNI)

类型/命名一致性:`addSnapshot`/`removeSnapshot`/`renameSnapshot`、`buildComparison`、`SnapshotComparison`、`PlanSnapshot` 在各任务间命名统一。
