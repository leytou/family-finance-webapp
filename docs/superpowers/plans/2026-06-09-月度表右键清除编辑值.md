# 月度表右键清除下方编辑值 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在月度表可编辑列（现金流列、余额列）的单元格上右键，弹出自定义菜单，提供"清除下方编辑值"功能，清除当前行严格下方该列所有编辑过的值。

**Architecture:** 新建通用 `ContextMenu.vue` 浮层组件，负责定位与渲染菜单项；业务逻辑（统计/清除下方编辑值）保留在 `MonthlyTable.vue`。现金流列调用 `store.updateColumnEntry(colId, month, null)`，余额列调用 `store.removeAnchor(month)`。store 现有方法已满足，无需改动。

**Tech Stack:** Vue 3 (Composition API + TypeScript)、Vitest + @vue/test-utils、UnoCSS。

---

## File Structure

- **Create** `src/components/ContextMenu.vue` — 通用右键菜单浮层组件，`position: fixed` 定位，点击外部/Esc 关闭，渲染菜单项列表。
- **Create** `tests/components/ContextMenu.spec.ts` — ContextMenu 组件测试。
- **Modify** `src/components/MonthlyTable.vue` — 在可编辑单元格监听 `@contextmenu.prevent`，维护右键菜单状态，计算菜单项与清除逻辑，挂载 `ContextMenu`。
- **Modify** `tests/components/MonthlyTable.spec.ts` — 新增右键清除相关测试。

---

## Task 1: 创建通用 ContextMenu 组件

**Files:**
- Create: `src/components/ContextMenu.vue`
- Test: `tests/components/ContextMenu.spec.ts`

ContextMenu 的接口约定：

```typescript
interface MenuItem {
  label: string
  disabled?: boolean
  onClick: () => void
}
```

Props：`x: number`、`y: number`、`items: MenuItem[]`。Emits：`close: []`。

行为：
- 用 `position: fixed`，`left/top` 绑定到 `x/y`。
- 点击某菜单项：若 `disabled` 则忽略；否则调用其 `onClick` 并 `emit('close')`。
- 点击组件外部关闭（复用 `useClickOutside`）。
- 按 Esc 关闭。

- [ ] **Step 1: 写失败的测试**

创建 `tests/components/ContextMenu.spec.ts`：

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { mount } from '@vue/test-utils'
import ContextMenu from '../../src/components/ContextMenu.vue'

describe('ContextMenu', () => {
  beforeEach(() => {
    vi.restoreAllMocks()
  })

  afterEach(() => {
    vi.restoreAllMocks()
  })

  it('渲染菜单项并定位到指定坐标', () => {
    const wrapper = mount(ContextMenu, {
      props: {
        x: 120,
        y: 80,
        items: [{ label: '清除下方编辑值', onClick: () => {} }],
      },
    })

    const menu = wrapper.get('[role="menu"]')
    expect(menu.text()).toContain('清除下方编辑值')
    const style = menu.attributes('style') ?? ''
    expect(style).toContain('left: 120px')
    expect(style).toContain('top: 80px')
  })

  it('点击可用菜单项触发 onClick 并 emit close', async () => {
    const onClick = vi.fn()
    const wrapper = mount(ContextMenu, {
      props: {
        x: 0,
        y: 0,
        items: [{ label: '清除下方编辑值', onClick }],
      },
    })

    await wrapper.get('[role="menuitem"]').trigger('click')

    expect(onClick).toHaveBeenCalledTimes(1)
    expect(wrapper.emitted('close')).toHaveLength(1)
  })

  it('点击禁用菜单项不触发 onClick 也不关闭', async () => {
    const onClick = vi.fn()
    const wrapper = mount(ContextMenu, {
      props: {
        x: 0,
        y: 0,
        items: [{ label: '清除下方编辑值', disabled: true, onClick }],
      },
    })

    const item = wrapper.get('[role="menuitem"]')
    expect(item.attributes('aria-disabled')).toBe('true')

    await item.trigger('click')

    expect(onClick).not.toHaveBeenCalled()
    expect(wrapper.emitted('close')).toBeUndefined()
  })

  it('按 Esc 时 emit close', async () => {
    const wrapper = mount(ContextMenu, {
      props: {
        x: 0,
        y: 0,
        items: [{ label: '清除下方编辑值', onClick: () => {} }],
      },
      attachTo: document.body,
    })

    await wrapper.get('[role="menu"]').trigger('keyup.escape')

    expect(wrapper.emitted('close')).toHaveLength(1)
    wrapper.unmount()
  })
})
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `npm run test -- ContextMenu`
Expected: FAIL，提示无法解析 `../../src/components/ContextMenu.vue`（文件不存在）。

- [ ] **Step 3: 实现 ContextMenu 组件**

创建 `src/components/ContextMenu.vue`：

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { useClickOutside } from '../composables/useClickOutside'

export interface MenuItem {
  label: string
  disabled?: boolean
  onClick: () => void
}

defineProps<{
  x: number
  y: number
  items: MenuItem[]
}>()

const emit = defineEmits<{
  close: []
}>()

const menuRef = ref<HTMLElement | null>(null)

function handleItemClick(item: MenuItem) {
  if (item.disabled) return
  item.onClick()
  emit('close')
}

// 点击菜单外部关闭（mousedown 捕获阶段，先于其他点击处理）
useClickOutside(menuRef, () => emit('close'))
</script>

<template>
  <div
    ref="menuRef"
    role="menu"
    class="fixed z-50 min-w-32 border rounded bg-white py-1 text-[11px] shadow-lg"
    :style="{ left: `${x}px`, top: `${y}px` }"
    tabindex="-1"
    @keyup.escape="emit('close')"
  >
    <button
      v-for="(item, idx) in items"
      :key="idx"
      type="button"
      role="menuitem"
      class="block w-full px-3 py-1 text-left whitespace-nowrap hover:bg-gray-100 disabled:cursor-not-allowed disabled:text-gray-400 disabled:hover:bg-transparent"
      :disabled="item.disabled"
      :aria-disabled="item.disabled ? 'true' : 'false'"
      @click="handleItemClick(item)"
    >
      {{ item.label }}
    </button>
  </div>
</template>
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `npm run test -- ContextMenu`
Expected: PASS，4 个测试全部通过。

- [ ] **Step 5: 提交**

```bash
git add src/components/ContextMenu.vue tests/components/ContextMenu.spec.ts
git commit -m "feat: 添加通用右键菜单 ContextMenu 组件"
```

---

## Task 2: MonthlyTable 接入右键清除逻辑

**Files:**
- Modify: `src/components/MonthlyTable.vue`
- Test: `tests/components/MonthlyTable.spec.ts`

设计要点：
- 用一个特殊常量标识余额列。现金流列用其 `columnId`，余额列用 `'__balance__'`。
- 右键菜单状态 `contextMenu` 记录 `{ columnId, month, x, y }`。
- 统计"下方编辑值数量"：遍历 `props.results` 中 `month > 当前月` 的行：
  - 现金流列：该列 `getColumnValue(result, columnId).isEdited` 为 true 计数。
  - 余额列：`result.isAnchor` 为 true 计数。
- 清除：对上述匹配的行，现金流列调用 `store.updateColumnEntry(columnId, month, null)`，余额列调用 `store.removeAnchor(month)`。
- 菜单项：`label: '清除下方编辑值'`，`disabled: count === 0`。

- [ ] **Step 1: 写失败的测试**

在 `tests/components/MonthlyTable.spec.ts` 的 `describe('MonthlyTable', ...)` 内、最后一个 `it(...)` 之后追加以下测试。注意复用文件顶部已有的 `loadUseStore`、`createResult`、`calculate` 导入。

```typescript
  it('右键现金流单元格弹出菜单', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    store.data.value.systemParams.startMonth = 202601

    const col = store.addColumn('测试列')
    store.updateColumnEntry(col.id, 202602, 5000)
    const results = calculate(store.data.value).slice(0, 3)

    const wrapper = mount(MonthlyTable, { props: { results } })

    // 第一行第一个现金流单元格（月份列之后）
    const firstRowCells = wrapper.findAll('tbody tr')[0].findAll('td')
    await firstRowCells[1].trigger('contextmenu')

    expect(wrapper.findComponent({ name: 'ContextMenu' }).exists()).toBe(true)
    expect(wrapper.text()).toContain('清除下方编辑值')
  })

  it('清除现金流列下方编辑值，当前行及上方保留', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    store.data.value.systemParams.startMonth = 202601

    const col = store.addColumn('测试列')
    // 三个月都有编辑值
    store.updateColumnEntry(col.id, 202601, 1000)
    store.updateColumnEntry(col.id, 202602, 2000)
    store.updateColumnEntry(col.id, 202603, 3000)
    const results = calculate(store.data.value).slice(0, 3)

    const wrapper = mount(MonthlyTable, { props: { results } })

    // 在第二行（202602）该列单元格右键
    const secondRowCells = wrapper.findAll('tbody tr')[1].findAll('td')
    await secondRowCells[1].trigger('contextmenu')

    // 点击"清除下方编辑值"
    const menuItem = wrapper
      .findComponent({ name: 'ContextMenu' })
      .get('[role="menuitem"]')
    await menuItem.trigger('click')

    // 202601、202602 的编辑值保留，202603 被清除
    expect(col.entries[202601]).toBe(1000)
    expect(col.entries[202602]).toBe(2000)
    expect(col.entries[202603]).toBeUndefined()
  })

  it('清除余额列下方锚点，当前行及上方保留', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    store.data.value.systemParams.startMonth = 202601

    store.addColumn('测试列')
    store.addAnchor(202601, 10000)
    store.addAnchor(202602, 20000)
    store.addAnchor(202603, 30000)
    const results = calculate(store.data.value).slice(0, 3)

    const wrapper = mount(MonthlyTable, { props: { results } })

    // 第二行余额列（最后一列）右键
    const secondRowCells = wrapper.findAll('tbody tr')[1].findAll('td')
    await secondRowCells[secondRowCells.length - 1].trigger('contextmenu')

    const menuItem = wrapper
      .findComponent({ name: 'ContextMenu' })
      .get('[role="menuitem"]')
    await menuItem.trigger('click')

    // 202601、202602 锚点保留，202603 被清除
    const months = store.data.value.anchors.map((a) => a.month)
    expect(months).toContain(202601)
    expect(months).toContain(202602)
    expect(months).not.toContain(202603)
  })

  it('下方无编辑值时菜单项禁用', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    store.data.value.systemParams.startMonth = 202601

    const col = store.addColumn('测试列')
    // 仅当前行有编辑值，下方无
    store.updateColumnEntry(col.id, 202603, 3000)
    const results = calculate(store.data.value).slice(0, 3)

    const wrapper = mount(MonthlyTable, { props: { results } })

    // 在最后一行（202603）该列右键，下方没有任何行
    const lastRowCells = wrapper.findAll('tbody tr')[2].findAll('td')
    await lastRowCells[1].trigger('contextmenu')

    const menuItem = wrapper
      .findComponent({ name: 'ContextMenu' })
      .get('[role="menuitem"]')
    expect(menuItem.attributes('aria-disabled')).toBe('true')
  })
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `npm run test -- MonthlyTable`
Expected: FAIL，新增 4 个测试失败（找不到 ContextMenu 组件 / contextmenu 无响应）。

- [ ] **Step 3: 在 MonthlyTable 的 `<script setup>` 中加入右键菜单逻辑**

在 `src/components/MonthlyTable.vue` 顶部 import 区，`FormulaPopover` 导入之后新增一行：

```typescript
import ContextMenu from './ContextMenu.vue'
```

在 `<script setup>` 中（例如紧接 `editCellOriginalValue` 定义之后、`startEditCell` 之前的位置）加入以下常量、状态与函数：

```typescript
// 余额列在右键菜单逻辑中的特殊列标识
const BALANCE_COLUMN_ID = '__balance__'

// 右键菜单状态：columnId 为现金流列 id，或 BALANCE_COLUMN_ID 表示余额列
const contextMenu = ref<{ columnId: string; month: number; x: number; y: number } | null>(null)

// 统计某列在指定月份"严格下方"编辑过的值的数量
function countEditedBelow(columnId: string, month: number): number {
  let count = 0
  for (const r of props.results) {
    if (r.month <= month) continue
    if (columnId === BALANCE_COLUMN_ID) {
      if (r.isAnchor) count++
    } else {
      if (getColumnValue(r, columnId).isEdited) count++
    }
  }
  return count
}

// 清除某列在指定月份"严格下方"所有编辑过的值
function clearEditedBelow(columnId: string, month: number): void {
  for (const r of props.results) {
    if (r.month <= month) continue
    if (columnId === BALANCE_COLUMN_ID) {
      if (r.isAnchor) store.removeAnchor(r.month)
    } else {
      if (getColumnValue(r, columnId).isEdited) store.updateColumnEntry(columnId, r.month, null)
    }
  }
}

// 打开右键菜单
function openContextMenu(columnId: string, month: number, event: MouseEvent): void {
  contextMenu.value = { columnId, month, x: event.clientX, y: event.clientY }
}

// 当前右键菜单的菜单项
const contextMenuItems = computed(() => {
  const ctx = contextMenu.value
  if (!ctx) return []
  const count = countEditedBelow(ctx.columnId, ctx.month)
  return [
    {
      label: '清除下方编辑值',
      disabled: count === 0,
      onClick: () => clearEditedBelow(ctx.columnId, ctx.month),
    },
  ]
})
```

- [ ] **Step 4: 在模板中为可编辑单元格加 contextmenu 监听并挂载菜单**

在现金流列 `<td>`（`v-for="column in columns"` 那个，约 322-330 行 `class` 之后）的开标签上新增事件绑定：

```
            @contextmenu.prevent="openContextMenu(column.id, result.month, $event)"
```

在余额列 `<td>`（累计列，约 426-432 行 `:class` 之后）的开标签上新增事件绑定：

```
            @contextmenu.prevent="openContextMenu(BALANCE_COLUMN_ID, result.month, $event)"
```

在模板末尾 `FormulaPopover` 之后新增 ContextMenu 挂载：

```vue
  <ContextMenu
    v-if="contextMenu"
    :x="contextMenu.x"
    :y="contextMenu.y"
    :items="contextMenuItems"
    @close="contextMenu = null"
  />
```

- [ ] **Step 5: 运行测试，确认通过**

Run: `npm run test -- MonthlyTable`
Expected: PASS，原有测试与新增 4 个测试全部通过。

- [ ] **Step 6: 运行完整测试套件与构建**

Run: `npm run test`
Expected: 全部测试通过。

Run: `npm run build`
Expected: 构建成功，无 TypeScript 错误。

- [ ] **Step 7: 提交**

```bash
git add src/components/MonthlyTable.vue tests/components/MonthlyTable.spec.ts
git commit -m "feat: 月度表可编辑列支持右键清除下方编辑值"
```

---

## Self-Review 备注

- **Spec 覆盖**：严格下方（`r.month <= month` 跳过当前及上方）、仅当前列（按 `columnId` 区分）、余额列支持（`BALANCE_COLUMN_ID` + `removeAnchor`）、无确认（点击即清）、菜单名"清除下方编辑值"、无值禁用（`count === 0`）——全部对应到 Task 1/2 的步骤与测试。
- **类型一致性**：`MenuItem`、`BALANCE_COLUMN_ID`、`countEditedBelow`、`clearEditedBelow`、`openContextMenu`、`contextMenuItems` 在 Task 1 定义、Task 2 引用，命名一致。
- **依赖**：`getColumnValue`、`store.updateColumnEntry`、`store.removeAnchor`、`useClickOutside` 均为现有符号，无需新增 store 方法。
