# 年度重复金额（每年此月）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让「每年固定月份、金额基本固定」的现金流项一次录入即覆盖所有年份同月，中间月不被蔓延，无需逐月填 0。

**Architecture:** `FlowColumn` 新增正交可选字段 `yearlyMonths`（`entries` 保持纯数字零改动）；`resolveColumnValue` 向前延续时跳过被标记 yearly 的月；新增 `store.syncYearly` 由右键「同步到每年此月」触发，把当月值快照式复制到投影范围内所有同月。直接编辑单元格只改当月、不波及其它年份。

**Tech Stack:** Vue 3 (Composition API + TypeScript)、UnoCSS、Vitest、localStorage。

**对应设计文档：** `docs/superpowers/specs/2026-06-12-年度重复金额-design.md`

---

## 文件结构

| 文件 | 责任 | 本计划改动 |
|---|---|---|
| src/types.ts | 核心类型 | `FlowColumn` 加可选 `yearlyMonths` |
| src/composables/useCalculation.ts | 解析与计算 | `resolveColumnValue` 延续时跳过 yearly 月 |
| src/composables/useStore.ts | 状态与持久化 | 新增 `syncYearly`；`updateColumnEntry` 删除联动清标记；`isValidColumn` 校验 `yearlyMonths` |
| src/components/MonthlyTable.vue | 月度表 UI | 右键菜单加「同步到每年此月」；yearly 单元格 `↻` 角标 |
| tests/composables/useCalculation.spec.ts | 解析测试 | 扩展 `resolveColumnValue` yearly 用例 |
| tests/composables/useStore.spec.ts | store 测试 | 新增 `syncYearly` / 联动清标记测试 |
| tests/components/MonthlyTable.spec.ts | 组件测试 | 新增菜单/角标用例；更新 2 个现有现金流右键用例 |

---

## Task 1: 数据模型字段 + resolveColumnValue 解析

**Files:**
- Modify: `src/types.ts`（`FlowColumn` 接口）
- Modify: `src/composables/useCalculation.ts:12-54`（`resolveColumnValue`）
- Test: `tests/composables/useCalculation.spec.ts`（`describe('resolveColumnValue')` 块）

- [ ] **Step 1: 在 `describe('resolveColumnValue')` 块末尾追加失败测试**

在 `tests/composables/useCalculation.spec.ts` 的 `resolveColumnValue` describe 块内（第 164 行 `})` 之前）追加：

```ts
  it('yearly 标记的月不向前延续，其后非同月归 0', () => {
    const column: FlowColumn = {
      id: 'col1',
      name: '年终奖',
      entries: { 202612: 50000 },
      yearlyMonths: { 202612: true },
    }

    // 202612 自身：直接 entry
    expect(resolveColumnValue(column, 202612)).toEqual({
      id: 'col1', name: '年终奖', amount: 50000, isEdited: true,
    })
    // 202701：跳过 yearly 的 202612，无源 → 0
    expect(resolveColumnValue(column, 202701)).toEqual({
      id: 'col1', name: '年终奖', amount: 0, isEdited: false,
    })
    // 202706：仍跳过 → 0
    expect(resolveColumnValue(column, 202706)).toEqual({
      id: 'col1', name: '年终奖', amount: 0, isEdited: false,
    })
  })

  it('无 yearlyMonths 字段时延续行为不变（回归）', () => {
    const column: FlowColumn = {
      id: 'col1', name: '工资', entries: { 202601: 10000 },
    }
    expect(resolveColumnValue(column, 202602)).toEqual({
      id: 'col1', name: '工资', amount: 10000, isEdited: false,
    })
  })

  it('yearly 与非 yearly 混合：非 yearly 仍延续，yearly 不作为延续源', () => {
    const column: FlowColumn = {
      id: 'col1',
      name: '混合',
      entries: { 202601: 1000, 202612: 50000 },
      yearlyMonths: { 202612: true },
    }
    // 202602 延续非 yearly 的 202601
    expect(resolveColumnValue(column, 202602).amount).toBe(1000)
    // 202612 自身 yearly 值
    expect(resolveColumnValue(column, 202612).amount).toBe(50000)
    // 202701：跳过 yearly 202612，回退到非 yearly 202601 → 1000
    expect(resolveColumnValue(column, 202701).amount).toBe(1000)
  })
```

- [ ] **Step 2: 运行测试验证失败**

Run: `npx vitest run tests/composables/useCalculation.spec.ts -t "resolveColumnValue"`
Expected: FAIL。「yearly 标记的月不向前延续」用例中 202701 实际得到 50000（被当作延续源），期望 0。

- [ ] **Step 3: `src/types.ts` 给 `FlowColumn` 加可选字段**

```ts
export interface FlowColumn {
  id: string
  name: string
  entries: Record<number, number>          // 稀疏存储，key=YYYYMM，只存用户手动编辑的值
  yearlyMonths?: Record<number, true>      // 标记哪些月是「年度重复项」(key=YYYYMM，存在即标记)；标记月不向前延续
}
```

- [ ] **Step 4: 修改 `resolveColumnValue` 延续时跳过 yearly 月**

将 `src/composables/useCalculation.ts` 中 `resolveColumnValue` 的规则 2（向前查找最近 entry）改为过滤掉 yearly 月：

```ts
  // 规则2: 向前查找最近的 entry（key < month），跳过被标记为 yearly 的月
  // yearly 月是脉冲项，不向前延续；否则其后所有月会被错误蔓延
  const isYearlyKey = (k: number) => Boolean(column.yearlyMonths?.[k])
  const entryKeys = Object.keys(column.entries)
    .map(Number)
    .filter(key => key < month && !isYearlyKey(key))
```

（仅替换原 `const entryKeys = ...filter(key => key < month)` 那一行及其上方注释，规则 1、3 与 `Math.max(...entryKeys)` 取值逻辑不变。）

- [ ] **Step 5: 运行测试验证通过**

Run: `npx vitest run tests/composables/useCalculation.spec.ts`
Expected: PASS（全部用例，含新 3 条与原有用例无回归）。

- [ ] **Step 6: 提交**

```bash
git add src/types.ts src/composables/useCalculation.ts tests/composables/useCalculation.spec.ts
git commit -m "feat: resolveColumnValue 跳过 yearly 月，支持年度脉冲项"
```

---

## Task 2: useStore — syncYearly + 删除联动 + 校验

**Files:**
- Modify: `src/composables/useStore.ts`（import、`updateColumnEntry`、新增 `syncYearly`、`isValidColumn`、return）
- Test: `tests/composables/useStore.spec.ts`

- [ ] **Step 1: 在 `describe('useStore')` 块内追加失败测试**

在 `tests/composables/useStore.spec.ts` 中（`describe('moveColumn')` 之前插入一个新 describe 块）：

```ts
  describe('syncYearly', () => {
    it('把当前月值写入投影范围内所有同月并标记 yearly，源头也标记', async () => {
      const useStore = await loadUseStore()
      const store = useStore()
      store.data.value.systemParams.startMonth = 202601
      const col = store.addColumn('年终奖')
      store.updateColumnEntry(col.id, 202612, 50000)

      store.syncYearly(col.id, 202612)

      const column = store.data.value.columns[0]
      // 投影范围 202601..203012 内所有 12 月
      expect(column.entries[202612]).toBe(50000)
      expect(column.entries[202712]).toBe(50000)
      expect(column.entries[202812]).toBe(50000)
      expect(column.entries[202912]).toBe(50000)
      expect(column.entries[203012]).toBe(50000)
      expect(column.yearlyMonths?.[202612]).toBe(true)
      expect(column.yearlyMonths?.[202712]).toBe(true)
      expect(column.yearlyMonths?.[203012]).toBe(true)
    })

    it('对无值的月不操作', async () => {
      const useStore = await loadUseStore()
      const store = useStore()
      store.data.value.systemParams.startMonth = 202601
      const col = store.addColumn('年终奖')
      // 202612 没有 entry
      store.syncYearly(col.id, 202612)
      const column = store.data.value.columns[0]
      expect(column.entries[202612]).toBeUndefined()
      expect(column.yearlyMonths).toBeUndefined()
    })

    it('不存在的列不抛错', async () => {
      const useStore = await loadUseStore()
      const store = useStore()
      expect(() => store.syncYearly('nonexistent', 202612)).not.toThrow()
    })

    it('直接修改某月 entry 不影响其它年份同月（快照语义）', async () => {
      const useStore = await loadUseStore()
      const store = useStore()
      store.data.value.systemParams.startMonth = 202601
      const col = store.addColumn('年终奖')
      store.updateColumnEntry(col.id, 202612, 50000)
      store.syncYearly(col.id, 202612)

      // 单独改 202712，其它年份不变
      store.updateColumnEntry(col.id, 202712, 70000)
      const column = store.data.value.columns[0]
      expect(column.entries[202612]).toBe(50000)
      expect(column.entries[202712]).toBe(70000)
      expect(column.entries[202812]).toBe(50000)
    })
  })
```

并在现有 `describe('updateColumnEntry ...')` 相关位置（紧接 `it('updateColumnEntry 不存在的列不抛错', ...)` 之后）追加联动测试：

```ts
  it('updateColumnEntry 删除 entry 时联动清除 yearlyMonths 标记', async () => {
    const useStore = await loadUseStore()
    const store = useStore()
    store.data.value.systemParams.startMonth = 202601
    const col = store.addColumn('年终奖')
    store.updateColumnEntry(col.id, 202612, 50000)
    store.syncYearly(col.id, 202612)
    const column = store.data.value.columns[0]
    expect(column.yearlyMonths?.[202612]).toBe(true)

    store.updateColumnEntry(col.id, 202612, null)
    expect(column.entries[202612]).toBeUndefined()
    expect(column.yearlyMonths?.[202612]).toBeUndefined()
  })
```

- [ ] **Step 2: 运行测试验证失败**

Run: `npx vitest run tests/composables/useStore.spec.ts -t "syncYearly"`
Expected: FAIL，`store.syncYearly is not a function`。

- [ ] **Step 3: 补 import `addMonths`**

`src/composables/useStore.ts` 第 3 行改为：

```ts
import { getCurrentMonth, formatMonth, addMonths } from '../utils/month'
```

- [ ] **Step 4: `updateColumnEntry` 删除分支联动清标记**

将 `src/composables/useStore.ts` 中 `updateColumnEntry` 改为：

```ts
  function updateColumnEntry(colId: string, month: number, value: number | null): void {
    const plan = getActivePlan()
    const column = plan.columns.find(col => col.id === colId)
    if (!column) return

    if (value === null) {
      delete column.entries[month]
      // 联动清除年度标记：entry 已不存在，标记无意义且会残留
      if (column.yearlyMonths) delete column.yearlyMonths[month]
    } else {
      column.entries[month] = value
    }
  }
```

- [ ] **Step 5: 新增 `syncYearly` 方法**

在 `updateColumnEntry` 之后插入：

```ts
  // 把指定月（须存在直接编辑值）的金额复制到投影范围内所有同月，并标记为 yearly。
  // 快照式：每个年份同月是独立 entry，之后单独修改某月不影响其它年份。
  function syncYearly(colId: string, month: number): void {
    const plan = getActivePlan()
    const column = plan.columns.find(col => col.id === colId)
    if (!column) return
    const amount = column.entries[month]
    if (amount === undefined) return                 // 无值不可同步
    if (!column.yearlyMonths) column.yearlyMonths = {}
    const moy = month % 100
    const start = plan.systemParams.startMonth
    for (let i = 0; i < 60; i++) {                   // 与 calculate 的 PROJECTION_MONTHS 一致
      const m = addMonths(start, i)
      if (m % 100 === moy) {
        column.entries[m] = amount
        column.yearlyMonths[m] = true
      }
    }
  }
```

- [ ] **Step 6: 在返回对象中导出 `syncYearly`**

在 `useStore()` 的 `return { ... }` 中，紧跟 `updateColumnEntry,` 之后加一行：

```ts
    updateColumnEntry,
    syncYearly,
    addAnchor,
```

- [ ] **Step 7: `isValidColumn` 校验可选 `yearlyMonths`**

在 `src/composables/useStore.ts` 的 `isValidColumn` 函数末尾 `return true` 之前插入：

```ts
  if (value.yearlyMonths !== undefined) {
    if (!isObject(value.yearlyMonths)) return false
    for (const key in value.yearlyMonths) {
      if (!Number.isInteger(Number(key))) return false
    }
  }
```

- [ ] **Step 8: 运行测试验证通过**

Run: `npx vitest run tests/composables/useStore.spec.ts`
Expected: PASS（含新 syncYearly 用例、联动清标记用例，原有用例无回归）。

- [ ] **Step 9: 提交**

```bash
git add src/composables/useStore.ts tests/composables/useStore.spec.ts
git commit -m "feat: 新增 syncYearly 与删除联动清标记，支持年度重复金额"
```

---

## Task 3: MonthlyTable 右键菜单 + 角标 + 更新现有测试

**Files:**
- Modify: `src/components/MonthlyTable.vue`（`contextMenuItems`、现金流单元格模板）
- Test: `tests/components/MonthlyTable.spec.ts`

- [ ] **Step 1: 更新两个现有现金流右键测试，按文本定位「清除下方编辑值」**

现有测试用 `.get('[role="menuitem"]')` 取第一项；新增「同步到每年此月」(排在清除之前) 后第一项会变。改用按文本定位。

在 `tests/components/MonthlyTable.spec.ts` 的 `it('清除现金流列下方编辑值，当前行及上方保留', ...)` 中，把菜单项定位段：

```ts
    // 点击"清除下方编辑值"
    const menuItem = wrapper
      .findComponent({ name: 'ContextMenu' })
      .get('[role="menuitem"]')
    await menuItem.trigger('click')
```

改为：

```ts
    // 点击"清除下方编辑值"（菜单已含"同步到每年此月"，按文本精确定位）
    const menuItem = wrapper
      .findComponent({ name: 'ContextMenu' })
      .findAll('[role="menuitem"]')
      .find(i => i.text() === '清除下方编辑值')!
    await menuItem.trigger('click')
```

同样，在 `it('下方无编辑值时菜单项禁用', ...)` 中，把：

```ts
    const menuItem = wrapper
      .findComponent({ name: 'ContextMenu' })
      .get('[role="menuitem"]')
    expect(menuItem.attributes('aria-disabled')).toBe('true')
```

改为：

```ts
    const menuItem = wrapper
      .findComponent({ name: 'ContextMenu' })
      .findAll('[role="menuitem"]')
      .find(i => i.text() === '清除下方编辑值')!
    expect(menuItem.attributes('aria-disabled')).toBe('true')
```

> 注：「清除余额列下方锚点」用例不改——余额列菜单不含「同步到每年此月」，第一项仍是清除。

- [ ] **Step 2: 追加新 UI 测试**

在 `tests/components/MonthlyTable.spec.ts` 中 `it('右键现金流单元格弹出菜单', ...)` 之后插入：

```ts
  it('右键有直接编辑值的现金流单元格，菜单含「同步到每年此月」且启用', async () => {
    const store = useSharedStore()
    store.reset()
    store.data.value.systemParams.startMonth = 202601
    const col = store.addColumn('年终奖')
    store.updateColumnEntry(col.id, 202612, 50000)
    const results = calculate(store.data.value).slice(0, 12) // 到 202612（索引 11）

    const wrapper = mount(MonthlyTable, { props: { results } })
    const row = wrapper.findAll('tbody tr')[11]
    await row.findAll('td')[1].trigger('contextmenu') // 该列单元格

    const menu = wrapper.findComponent({ name: 'ContextMenu' })
    expect(menu.text()).toContain('同步到每年此月')
    const syncItem = menu.findAll('[role="menuitem"]').find(i => i.text() === '同步到每年此月')!
    expect(syncItem.attributes('aria-disabled')).toBe('false')
  })

  it('右键无直接编辑值的现金流单元格，「同步到每年此月」禁用', async () => {
    const store = useSharedStore()
    store.reset()
    store.data.value.systemParams.startMonth = 202601
    store.addColumn('年终奖')
    store.updateColumnEntry(store.data.value.columns[0].id, 202601, 50000) // 202601 有值，202602 为延续值
    const results = calculate(store.data.value).slice(0, 3)

    const wrapper = mount(MonthlyTable, { props: { results } })
    const row = wrapper.findAll('tbody tr')[1] // 202602，延续值无直接 entry
    await row.findAll('td')[1].trigger('contextmenu')

    const menu = wrapper.findComponent({ name: 'ContextMenu' })
    const syncItem = menu.findAll('[role="menuitem"]').find(i => i.text() === '同步到每年此月')!
    expect(syncItem.attributes('aria-disabled')).toBe('true')
  })

  it('点击「同步到每年此月」后该列所有年份同月都有值并标记', async () => {
    const store = useSharedStore()
    store.reset()
    store.data.value.systemParams.startMonth = 202601
    const col = store.addColumn('年终奖')
    store.updateColumnEntry(col.id, 202612, 50000)
    const results = calculate(store.data.value).slice(0, 12)

    const wrapper = mount(MonthlyTable, { props: { results } })
    const row = wrapper.findAll('tbody tr')[11]
    await row.findAll('td')[1].trigger('contextmenu')

    const menu = wrapper.findComponent({ name: 'ContextMenu' })
    const syncItem = menu.findAll('[role="menuitem"]').find(i => i.text() === '同步到每年此月')!
    await syncItem.trigger('click')

    expect(col.entries[202712]).toBe(50000)
    expect(col.entries[203012]).toBe(50000)
    expect(col.yearlyMonths?.[202612]).toBe(true)
    expect(col.yearlyMonths?.[202712]).toBe(true)
  })

  it('yearly 单元格显示 ↻ 角标', async () => {
    const store = useSharedStore()
    store.reset()
    store.data.value.systemParams.startMonth = 202601
    const col = store.addColumn('年终奖')
    store.updateColumnEntry(col.id, 202612, 50000)
    store.syncYearly(col.id, 202612)
    const results = calculate(store.data.value).slice(0, 12)

    const wrapper = mount(MonthlyTable, { props: { results } })
    const row = wrapper.findAll('tbody tr')[11] // 202612
    expect(row.text()).toContain('↻')
  })

  it('余额列右键菜单不含「同步到每年此月」', async () => {
    const store = useSharedStore()
    store.reset()
    store.data.value.systemParams.startMonth = 202601
    store.addColumn('测试列')
    const results = calculate(store.data.value).slice(0, 3)

    const wrapper = mount(MonthlyTable, { props: { results } })
    const row = wrapper.findAll('tbody tr')[0]
    const cells = row.findAll('td')
    await cells[cells.length - 1].trigger('contextmenu') // 余额列（最后一列）

    const menu = wrapper.findComponent({ name: 'ContextMenu' })
    expect(menu.text()).not.toContain('同步到每年此月')
  })
```

- [ ] **Step 3: 运行测试验证失败**

Run: `npx vitest run tests/components/MonthlyTable.spec.ts`
Expected: FAIL——新用例找不到「同步到每年此月」菜单项 / `↻` 角标；（若 Step 1 已改，两个更新用例应已能通过。）

- [ ] **Step 4: 改 `contextMenuItems` 加入「同步到每年此月」**

将 `src/components/MonthlyTable.vue` 的 `contextMenuItems` computed 改为：

```ts
// 当前右键菜单的菜单项
const contextMenuItems = computed(() => {
  const ctx = contextMenu.value
  if (!ctx) return []
  const items: { label: string; disabled?: boolean; onClick: () => void }[] = []

  // 同步到每年此月：仅现金流列，且该月存在直接编辑值时启用
  if (ctx.columnId !== BALANCE_COLUMN_ID) {
    const column = columns.value.find(c => c.id === ctx.columnId)
    const hasDirectEntry = column ? String(ctx.month) in column.entries : false
    items.push({
      label: '同步到每年此月',
      disabled: !hasDirectEntry,
      onClick: () => store.syncYearly(ctx.columnId, ctx.month),
    })
  }

  const count = countEditedBelow(ctx.columnId, ctx.month)
  items.push({
    label: '清除下方编辑值',
    disabled: count === 0,
    onClick: () => clearEditedBelow(ctx.columnId, ctx.month),
  })

  return items
})
```

- [ ] **Step 5: 模板加 `↻` 角标**

在 `src/components/MonthlyTable.vue` 现金流列单元格的显示 `<span>` 内（`{{ formatCurrency(getColumnValue(result, column.id).amount) }}` 之后）追加角标：

```html
            <span
              v-else
              class="block w-full cursor-pointer"
              :aria-label="`编辑 ${formatMonth(result.month)} ${column.name}`"
              @click="startEditCell(column.id, result.month, getColumnValue(result, column.id).amount)"
            >
              {{ formatCurrency(getColumnValue(result, column.id).amount) }}<span
                v-if="column.yearlyMonths?.[result.month]"
                class="ml-0.5 text-blue-500"
                aria-hidden="true"
              >↻</span>
            </span>
```

- [ ] **Step 6: 运行测试验证通过**

Run: `npx vitest run tests/components/MonthlyTable.spec.ts`
Expected: PASS（含 5 条新用例与 2 条更新用例，其余无回归）。

- [ ] **Step 7: 全量测试 + 类型检查**

Run: `npm run test`
Expected: 全部通过。

如有 `tsc`/类型检查脚本则一并运行，确保 `yearlyMonths?: Record<number, true>` 与各处访问类型正确。

- [ ] **Step 8: 提交**

```bash
git add src/components/MonthlyTable.vue tests/components/MonthlyTable.spec.ts
git commit -m "feat: 月度表右键支持同步到每年此月并显示年度角标"
```

---

## 完成后人工验证

启动 `npm run dev`，在月度表：
1. 新建列「年终奖」，在 202612 填 50000 → 右键 → 同步到每年此月。
2. 观察 202712、202812… 自动为 50000 且带 `↻`，202701–202711 为 0（无需填 0）。
3. 改 202712 为 70000 → 仅该月变化，其它年份仍 50000。
4. 清空 202612 → 该月归 0 且 `↻` 消失。
